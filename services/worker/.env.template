# Environment Configuration for Worker Service
NODE_ENV=production

# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=https://htxsylxwvcbrazdowjys.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Google Gemini API (Required)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini API URL (uses default if not specified)
# GEMINI_API_URL=https://generativelanguage.googleapis.com

# Optional: App URL (defaults to localhost:3000)
NEXT_PUBLIC_APP_URL=https://your-vercel-app-url.vercel.app

# Optional: Storage TTL settings (uses defaults if not specified)
# STORAGE_TTL_ORIGINALS_DAYS=30
# STORAGE_TTL_INTERMEDIATES_HOURS=48
# STORAGE_TTL_ARTIFACTS_DAYS=90