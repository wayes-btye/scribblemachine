{
  "mcpServers": {
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["@upstash/context7-mcp"],
      "env": {},
      "_comment": "Pre-installed globally for faster startup"
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp"],
      "env": {},
      "_comment": "Pre-installed globally for faster startup"
    },
    "shadcn": {
      "type": "stdio",
      "command": "npx",
      "args": ["shadcn", "mcp"],
      "env": {},
      "_comment": "Pre-installed globally for faster startup"
    }
  },
  "_disabled_servers": {
    "_note": "Supabase, Vercel, and Cloud Run MCPs temporarily disabled",
    "_reason": "Native dependency build issues in GitHub Actions CI",
    "_supabase_issue": "libpg-query native addon fails to build",
    "_todo": "Re-enable after resolving build issues or use alternative methods"
  }
}
